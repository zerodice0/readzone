{
  "name": "@readzone/backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "cd ../.. && convex dev",
    "build": "convex deploy --cmd 'echo Convex deployed'",
    "codegen": "convex codegen",
    "type-check": "tsc --noEmit",
    "lint": "eslint convex/"
  },
  "dependencies": {
    "@clerk/backend": "^2.27.0",
    "convex": "^1.30.0",
    "svix": "^1.82.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
